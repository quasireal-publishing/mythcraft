{{formGroup systemFields.quantity value=system.quantity disabled=@root.isPlay}}
{{formGroup systemFields.cost value=system.cost disabled=@root.isPlay}}
{{formGroup systemFields.category value=system.category disabled=@root.isPlay}}
{{formGroup systemFields.ar value=system.ar disabled=@root.isPlay}}
<fieldset disabled="@root.isPlay">
  <legend>{{systemFields.defenses.label}}</legend>
</fieldset>
{{formGroup systemFields.strMin value=system.strMin disabled=@root.isPlay}}
{{formGroup systemFields.dexMax value=system.dexMax disabled=@root.isPlay}}
{{formGroup systemFields.speedPenalty value=system.speedPenalty disabled=@root.isPlay}}
{{!-- resist --}}
