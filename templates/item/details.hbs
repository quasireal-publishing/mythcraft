<section class="tab scrollable {{tab.cssClass}} standard-form" data-group="primary" data-tab="details">
  {{formGroup systemFields.properties value=system.properties options=propertyOptions localize=true type="checkboxes" classes="stacked"}}
  {{#if systemFields.level}}
  {{formGroup systemFields.level value=system.level}}
  {{/if}}
  {{#if systemFields.category}}
  {{formGroup systemFields.category value=system.category}}
  {{/if}}
</section>
