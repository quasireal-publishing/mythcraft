{{! Effects Tab }}
<section class="tab effects flexcol scrollable {{tab.cssClass}}" data-group="primary" data-tab="effects">
  <ul class="effects-list">
    {{#each effects as |section sid|}}
    <li class="effects-header flexrow draggable" data-effect-type="{{section.type}}">
      <div class="effect-label flexrow">
        {{localize section.label}}
      </div>
      <div class="effect-source">
        {{localize "EFFECT.FIELDS.origin.label"}}
      </div>
      <div class="effect-duration">{{localize "EFFECT.TABS.duration"}}</div>
      <div class="effect-controls flexrow">
        <button
          type="button"
          class="effect-controls"
          data-action="createDoc" data-document-class="ActiveEffect"
          {{!-- eslint-ignore-next-line html-eslint/no-duplicate-attrs --}}
          {{#if (eq section.type "inactive" )}} data-disabled="true"
          {{else if (eq section.type "temporary" )}} data-duration.rounds="1" {{/if}}
          data-origin="{{@root.document.uuid}}"
          title="{{localize 'DOCUMENT.Create' type=(localize 'DOCUMENT.ActiveEffect')}}"
          data-img="icons/svg/aura.svg"
        >
          <i class="fas fa-plus"></i>
          {{localize "DOCUMENT.New" type=(localize "MYTHCRAFT.Effect.Label")}}
        </button>
      </div>
    </li>

    <ul class="effect-list">
      {{#each section.effects as |effect|}}
      <li class="effect flexrow draggable" data-effect-id="{{effect.id}}" data-parent-id="{{effect.parent.id}}"
        data-document-class="ActiveEffect">
        <div class="effect-label flexrow">
          <img class="effect-image" src="{{effect.img}}" alt="{{localize "EFFECT.FIELDS.img.label"}}" height="24" width="24">
          <div class="effect-name">{{effect.name}}</div>
        </div>
        <div class="effect-source">{{effect.sourceName}}</div>
        <div class="effect-duration">{{effect.duration.label}}</div>
        <div class="effect-controls flexrow">
          <button type="button" class="icon fa-solid fa-ellipsis-vertical"></button>
          <button type="button" data-action="toggleItemEmbed" class="icon fa-solid fa-angle-{{ifThen expanded "down" "right"}}"></button>
        </div>
      </li>
      {{/each}}
    </ul>
    {{/each}}
  </ul>
</section>
