
{{! Features Tab }}
<section class="tab scrollable {{tab.cssClass}}" data-group="primary" data-tab="features">
  {{#each features}}

  {{#if (eq @key "action")}}
  {{#if @root.isPlay}}
  {{{@root.enrichedActions}}}
  {{else}}
  {{formInput @root.systemFields.actions enriched=@root.enrichedActions value=@root.system.actions toggled="true"}}
  {{/if}}
  {{/if}}
  <div class="item-header flexrow">
    <div class="item-name">{{localize label}}</div>
    <div class="item-uses">{{localize "MYTHCRAFT.Item.feature.FIELDS.uses.label"}}</div>
    {{#if (eq @key "action")}}
    <div class="item-tier">{{localize "MYTHCRAFT.Item.feature.FIELDS.tier.label"}}</div>
    {{/if}}
    <div class="item-controls flexrow">
      {{#unless @root.isPlay}}
      <button
        type="button"
        class="item-create"
        data-action="createDoc"
        data-document-class="Item"
        data-type="feature"
        data-system.category="{{@key}}"
      >
        <i class="fa-solid fa-plus"></i>
        {{localize "DOCUMENT.Create" type=(localize "TYPES.Item.feature")}}
      </button>
      {{/unless}}
    </div>
  </div>
  <ul class="item-list {{@key}}">
    {{#each list}}
    <li class="item draggable" data-item-id="{{item.id}}" data-document-class="Item">
      <div class="item-row flexrow">
        <img src="{{item.img}}" alt="{{item.name}}">
        <div class="item-name">{{item.name}}</div>
        <div class="item-uses">
          {{#if item.system.uses.max}}
          <input
            type="number"
            class="item-input inline"
            data-name="system.uses.value"
            {{disabled (not @root.editable)}}
            value="{{item.system.uses.value}}"
          >
          <span>/ {{item.system.uses.max}}</span>
          {{/if}}
        </div>
        {{#if item.system.showTier}}
        <div class="item-tier">
          {{item.system.tier}}
        </div>
        {{/if}}
        <div class="item-controls flexrow">
          <button type="button" class="icon fa-solid fa-ellipsis-vertical"></button>
          <button type="button" data-action="toggleItemEmbed" class="icon fa-solid fa-angle-{{ifThen expanded "down" "right"}}"></button>
        </div>
      </div>
      <div class="item-embed">
        {{#if expanded}}{{{embed.outerHTML}}}{{/if}}
      </div>
    </li>
    {{/each}}
  </ul>
  {{/each}}
</section>
